name: Fix external merge configuration flow

on:
  push:
    branches:
      - dev_lcc
    paths:
      - .github/workflows/fix-external-merge-flow.yml

permissions:
  contents: write

jobs:
  apply-fix:
    runs-on: windows-latest
    steps:
      - name: Checkout dev_lcc
        uses: actions/checkout@v4
        with:
          ref: dev_lcc
          fetch-depth: 0

      - name: Apply source changes
        shell: pwsh
        run: |
          @'
          from pathlib import Path

          external_merge_view = r'''use std::{path::Path, sync::{Mutex, OnceLock}};

use gpui::{Context, IntoElement, Window, div, prelude::*, px, rgb};

use crate::{
    DialogState, FieldId, RepositoryView,
    ui::{components::dialog_actions, theme as ui_theme},
};

static PENDING_EXTERNAL_MERGE_PATH: OnceLock<Mutex<Option<String>>> = OnceLock::new();

fn pending_external_merge_path_cell() -> &'static Mutex<Option<String>> {
    PENDING_EXTERNAL_MERGE_PATH.get_or_init(|| Mutex::new(None))
}

fn pending_external_merge_path() -> Option<String> {
    pending_external_merge_path_cell()
        .lock()
        .ok()
        .and_then(|pending| pending.clone())
}

fn set_pending_external_merge_path(path: String) {
    if let Ok(mut pending) = pending_external_merge_path_cell().lock() {
        *pending = Some(path);
    }
}

fn take_pending_external_merge_path() -> Option<String> {
    pending_external_merge_path_cell()
        .lock()
        .ok()
        .and_then(|mut pending| pending.take())
}

fn clear_pending_external_merge_path() {
    if let Ok(mut pending) = pending_external_merge_path_cell().lock() {
        *pending = None;
    }
}

impl RepositoryView {
    pub(crate) fn render_external_merge_settings_dialog(
        &self,
        window: &Window,
        cx: &mut Context<Self>,
    ) -> impl IntoElement {
        let pending_path = pending_external_merge_path();
        let save_label = if pending_path.is_some() {
            "保存并继续"
        } else {
            "保存"
        };

        self.dialog_panel("合并工具", cx)
            .w(px(580.0))
            .when_some(pending_path, |this, path| {
                this.child(
                    div()
                        .px_3()
                        .py_2()
                        .rounded_sm()
                        .border_1()
                        .border_color(rgb(ui_theme::COLOR_WARNING))
                        .bg(rgb(ui_theme::COLOR_WARNING))
                        .text_size(px(12.0))
                        .line_height(px(18.0))
                        .text_color(rgb(ui_theme::COLOR_WARNING_FOREGROUND))
                        .child(format!(
                            "尚未找到可用的 IntelliJ IDEA。配置完成后将继续解决：{path}"
                        )),
                )
            })
            .child(self.toggle_row(
                "external-merge-enabled",
                "启用 IntelliJ IDEA 外部合并",
                self.external_merge_enabled_form,
                |this, _, _| {
                    this.set_external_merge_enabled_form_with_detection(
                        !this.external_merge_enabled_form,
                    );
                },
                cx,
            ))
            .child(self.toggle_row(
                "external-merge-auto-open",
                "选中冲突文件时自动打开 IDEA",
                self.external_merge_auto_open_form,
                |this, _, _| {
                    this.set_external_merge_auto_open_form_with_detection(
                        !this.external_merge_auto_open_form,
                    );
                },
                cx,
            ))
            .child(self.input(FieldId::ExternalMergeIntellijPath, false, window, cx))
            .child(self.button(
                "选择 IDEA 程序",
                !self.busy,
                |this, _, _| this.browse_external_merge_executable(),
                cx,
            ))
            .child(
                div()
                    .px_3()
                    .py_2()
                    .rounded_sm()
                    .border_1()
                    .border_color(rgb(ui_theme::BORDER))
                    .bg(rgb(ui_theme::CARD))
                    .text_size(px(12.0))
                    .line_height(px(18.0))
                    .text_color(rgb(ui_theme::MUTED_FOREGROUND))
                    .child("路径可留空。留空时会依次检测 KHASLANA_IDEA_PATH、PATH 中的 idea64 / idea，以及常见 JetBrains 安装目录。开启或保存时会立即验证；未找到工具时不会静默结束，而是保留当前操作并提示配置。"),
            )
            .when(self.last_error.is_some(), |this| {
                this.child(
                    div()
                        .text_size(px(12.0))
                        .text_color(rgb(ui_theme::DESTRUCTIVE))
                        .child(self.last_error.clone().unwrap_or_default()),
                )
            })
            .child(
                dialog_actions()
                    .child(self.button(
                        "取消",
                        !self.busy,
                        |this, _, _| this.cancel_external_merge_settings(),
                        cx,
                    ))
                    .child(self.button(
                        "检测 IDEA",
                        !self.busy,
                        |this, _, _| this.test_external_merge_settings_from_form(),
                        cx,
                    ))
                    .child(self.primary_button(
                        save_label,
                        !self.busy,
                        |this, _, _| this.save_external_merge_settings_from_form_and_resume(),
                        cx,
                    )),
            )
    }

    pub(crate) fn request_external_merge_for_path(&mut self, path: String) -> bool {
        let validation = if self.external_merge_settings.enabled {
            khaslana::external_merge::resolve_intellij_idea_command_with_settings(
                &self.external_merge_settings,
            )
        } else {
            Err(khaslana::GitError::Message("外部合并工具未启用".into()))
        };

        if let Err(err) = validation {
            set_pending_external_merge_path(path);
            self.open_external_merge_settings();
            if !self.external_merge_enabled_form {
                self.set_external_merge_enabled_form(true);
            }
            self.status = "需要配置 IntelliJ IDEA 合并工具".into();
            self.last_error = Some(format!(
                "无法启动外部合并工具：{err}。请选择 IDEA 程序路径，保存后会自动继续当前冲突。"
            ));
            return false;
        }

        self.start_external_merge_operation(path);
        true
    }

    fn start_external_merge_operation(&mut self, path: String) {
        let settings = self.external_merge_settings.clone();
        self.diff = None;
        self.diff_headers_expanded = false;
        self.reset_uniform_scroll("diff-scroll");
        self.with_repo_blocking(
            "IntelliJ IDEA 合并结果已应用",
            move |service, repo| {
                service.resolve_conflict_with_intellij_idea_settings(
                    repo,
                    Path::new(&path),
                    &settings,
                )
            },
        );
    }

    pub(crate) fn save_external_merge_settings_from_form_and_resume(&mut self) {
        let settings = self.external_merge_form_settings();
        let has_pending_merge = pending_external_merge_path().is_some();

        if has_pending_merge && !settings.enabled {
            self.last_error = Some("请先启用外部合并工具，再保存并继续当前冲突".into());
            return;
        }

        let resolved_path = if settings.enabled {
            match khaslana::external_merge::resolve_intellij_idea_command_with_settings(&settings) {
                Ok(path) => Some(path),
                Err(err) => {
                    self.status = "需要配置 IntelliJ IDEA 合并工具".into();
                    self.last_error = Some(format!(
                        "未找到可用的 IntelliJ IDEA：{err}。请填写或选择正确的程序路径。"
                    ));
                    return;
                }
            }
        } else {
            None
        };

        self.external_merge_settings = settings;
        self.save_external_merge_settings();
        self.status = resolved_path
            .map(|path| format!("合并工具设置已保存：{}", path.display()))
            .unwrap_or_else(|| "合并工具设置已保存".into());
        self.last_error = None;
        self.active_dialog = None;

        if let Some(path) = take_pending_external_merge_path() {
            self.start_external_merge_operation(path);
        }
    }

    pub(crate) fn cancel_external_merge_settings(&mut self) {
        clear_pending_external_merge_path();
        self.close_dialog();
    }

    pub(crate) fn set_external_merge_enabled_form_with_detection(&mut self, enabled: bool) {
        self.set_external_merge_enabled_form(enabled);
        if !enabled {
            return;
        }

        let settings = self.external_merge_form_settings();
        match khaslana::external_merge::resolve_intellij_idea_command_with_settings(&settings) {
            Ok(path) => {
                self.status = format!("已找到 IntelliJ IDEA：{}", path.display());
                self.last_error = None;
            }
            Err(err) => {
                self.status = "需要配置 IntelliJ IDEA 合并工具".into();
                self.last_error = Some(format!(
                    "启用前需要配置 IntelliJ IDEA：{err}。请填写路径或点击“选择 IDEA 程序”。"
                ));
            }
        }
    }

    pub(crate) fn set_external_merge_auto_open_form_with_detection(&mut self, enabled: bool) {
        self.set_external_merge_auto_open_form(enabled);
        if !enabled {
            return;
        }

        let settings = self.external_merge_form_settings();
        match khaslana::external_merge::resolve_intellij_idea_command_with_settings(&settings) {
            Ok(path) => {
                self.status = format!("已找到 IntelliJ IDEA：{}", path.display());
                self.last_error = None;
            }
            Err(err) => {
                self.status = "需要配置 IntelliJ IDEA 合并工具".into();
                self.last_error = Some(format!(
                    "开启自动打开前需要配置 IntelliJ IDEA：{err}。请填写路径或点击“选择 IDEA 程序”。"
                ));
            }
        }
    }

    pub(crate) fn browse_external_merge_executable(&mut self) {
        let dialog = rfd::FileDialog::new().set_title("选择 IntelliJ IDEA 启动程序");
        #[cfg(windows)]
        let dialog = dialog.add_filter("IntelliJ IDEA", &["exe", "bat", "cmd"]);

        if let Some(path) = dialog.pick_file() {
            self.external_merge_intellij_path
                .set_value(path.display().to_string());
            self.external_merge_enabled_form = true;
            self.status = format!("已选择 IntelliJ IDEA：{}", path.display());
            self.last_error = None;
        }
    }
}
'''

          Path("src/external_merge_view.rs").write_text(external_merge_view, encoding="utf-8")

          conflicts_path = Path("src/conflicts/mod.rs")
          conflicts = conflicts_path.read_text(encoding="utf-8")

          old_button = '''                    .child(self.button(
                        "用 IntelliJ IDEA 解决",
                        view.is_some() && self.external_merge_settings.enabled && !self.busy,
                        |this, _, _| this.resolve_selected_conflict_with_intellij_idea(),
                        cx,
                    ))'''
          new_button = '''                    .child(self.button(
                        if self.external_merge_settings.enabled {
                            "用 IntelliJ IDEA 解决"
                        } else {
                            "配置 IDEA 并解决"
                        },
                        view.is_some() && !self.busy,
                        |this, _, _| this.resolve_selected_conflict_with_intellij_idea(),
                        cx,
                    ))'''
          assert conflicts.count(old_button) == 1, "external merge button block changed unexpectedly"
          conflicts = conflicts.replace(old_button, new_button, 1)

          old_flow = '''    fn resolve_selected_conflict_with_intellij_idea(&mut self) {
        let Some(path) = self.conflict_workbench.selected_path.clone() else {
            self.last_error = Some("请先选择一个冲突文件".into());
            return;
        };
        self.resolve_conflict_with_intellij_idea_path(path);
    }

    fn resolve_conflict_with_intellij_idea_path(&mut self, path: String) {
        if !self.external_merge_settings.enabled {
            self.last_error = Some("外部合并工具未启用".into());
            return;
        }
        let settings = self.external_merge_settings.clone();
        self.diff = None;
        self.diff_headers_expanded = false;
        self.reset_uniform_scroll("diff-scroll");
        self.conflict_workbench.files.remove(&path);
        self.with_repo_blocking(
            "IntelliJ IDEA 合并结果已应用",
            move |service, repo| {
                service.resolve_conflict_with_intellij_idea_settings(
                    repo,
                    Path::new(&path),
                    &settings,
                )
            },
        );
    }

    pub(crate) fn maybe_auto_open_external_merge_for_selected_conflict(&mut self) {
        if self.busy
            || !self.external_merge_settings.enabled
            || !self.external_merge_settings.auto_open_intellij
        {
            return;
        }
        let Some(path) = self.conflict_workbench.selected_path.clone() else {
            return;
        };
        if !self.conflict_workbench.files.contains_key(&path) {
            return;
        }
        if self
            .conflict_workbench
            .mark_external_merge_auto_opened(path.clone())
        {
            self.resolve_conflict_with_intellij_idea_path(path);
        }
    }'''
          new_flow = '''    fn resolve_selected_conflict_with_intellij_idea(&mut self) {
        let Some(path) = self.conflict_workbench.selected_path.clone() else {
            self.last_error = Some("请先选择一个冲突文件".into());
            return;
        };
        self.resolve_conflict_with_intellij_idea_path(path);
    }

    fn resolve_conflict_with_intellij_idea_path(&mut self, path: String) -> bool {
        self.request_external_merge_for_path(path)
    }

    pub(crate) fn maybe_auto_open_external_merge_for_selected_conflict(&mut self) {
        if self.busy
            || !self.external_merge_settings.enabled
            || !self.external_merge_settings.auto_open_intellij
        {
            return;
        }
        let Some(path) = self.conflict_workbench.selected_path.clone() else {
            return;
        };
        if !self.conflict_workbench.files.contains_key(&path)
            || self
                .conflict_workbench
                .external_merge_auto_opened
                .contains(&path)
        {
            return;
        }
        if self.resolve_conflict_with_intellij_idea_path(path.clone()) {
            self.conflict_workbench
                .mark_external_merge_auto_opened(path);
        }
    }'''
          assert conflicts.count(old_flow) == 1, "external merge flow block changed unexpectedly"
          conflicts = conflicts.replace(old_flow, new_flow, 1)
          conflicts_path.write_text(conflicts, encoding="utf-8")

          main_path = Path("src/main.rs")
          main = main_path.read_text(encoding="utf-8")
          old_submit = '''        } else if matches!(field, FieldId::ExternalMergeIntellijPath) {
            if self.active_dialog == Some(DialogState::ExternalMergeSettings) {
                self.save_external_merge_settings_from_form();
            }
        } else if matches!('''
          new_submit = '''        } else if matches!(field, FieldId::ExternalMergeIntellijPath) {
            if self.active_dialog == Some(DialogState::ExternalMergeSettings) {
                self.save_external_merge_settings_from_form_and_resume();
            }
        } else if matches!('''
          assert main.count(old_submit) == 1, "external merge submit block changed unexpectedly"
          main = main.replace(old_submit, new_submit, 1)
          main_path.write_text(main, encoding="utf-8")

          workflow = Path(".github/workflows/fix-external-merge-flow.yml")
          if workflow.exists():
              workflow.unlink()
          '@ | python -

      - name: Format
        shell: pwsh
        run: cargo fmt --all

      - name: Verify formatting
        shell: pwsh
        run: cargo fmt --all -- --check

      - name: Compile check
        shell: pwsh
        run: cargo check --bin khaslana

      - name: Commit fix
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: guide external merge tool configuration"
          git push origin HEAD:dev_lcc
