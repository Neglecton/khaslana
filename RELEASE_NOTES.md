Khaslana 2.0.1-beta.1

2.0 之后的第一个测试版：侧边栏分组标题钉住、贮藏查看更顺手、工作流可绑定快捷键。

上下文导航

- 分组标题钉住常驻：本地分支、远端、远端分支、标签、贮藏的标题不再随列表滚动，条目再多也能一眼看到各分组入口；仅本地分支展开时，其余分组标题依次固定在区域下方
- 条目多的分组平分剩余空间，条目少的按内容定高，任何情况下标题不会被滚出视口；分支搜索框打开时也钉在标题下方

贮藏

- 左键点击贮藏条目即可直接查看，无需先右键再选「查看贮藏」；右键菜单入口保留
- 「弹出贮藏」新增确认弹窗，应用改动到工作区并从贮藏列表移除的后果一目了然

工作流快捷键

- 工作流模板可绑定快捷键：模板右键「绑定快捷键...」按下组合键即录入，任意数量模板可绑定，绑定后模板行右缘显示键位标记
- 可勾选「后台执行」（默认不勾选）：不勾选时触发后跳转到工作流页运行并实时查看时间线；勾选时停留在当前页后台运行，完成与失败以气泡提示
- 触发时始终读取模板的最新内容——改过的模板运行的一定是最新版本，已填写的运行变量自动保留；模板必填变量未填时跳转工作流页提示填写，不盲目运行
- 模板删除后绑定自动清理，改名后绑定跟随迁移，不留失效快捷键

快捷键冲突

- 统一冲突检查：录制工作流快捷键、重绑应用快捷键、「恢复默认」都会检测与其它动作或工作流绑定的键位冲突，占用时拒绝并提示占用方
- 设置中心「快捷键」页新增「工作流快捷键」分区，可查看与清除已绑定的工作流
