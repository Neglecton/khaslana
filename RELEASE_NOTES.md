Khaslana 2.0

全新界面与全面能力升级，包含 2.0 两个测试版（beta.1 / beta.2）的全部改进与后续修复。

全新界面

- 全新 Focus Workbench 界面：精简标题栏、可展开收起的上下文导航、设置中心整合全部设置分类（凭据 / 代理 / AI / 合并工具 / 外观 / 更新 / 快捷键 / 关于）
- 布局记忆：上下文导航展开状态与全部分割线位置跨会话保留；切换、打开、克隆仓库后停留在当前页面
- 通知气泡统一在右下角展示，每个气泡均可手动关闭
- 全新应用图标；深浅色主题与 9 种主题色即时切换
- 键盘交互收敛：仅保留可配置的应用快捷键与文本框内编辑操作，界面交互统一为鼠标

提交历史与图谱

- 新增「提交图谱」页：泳道拓扑展示、分支动向高亮（全谱系 / 仅领先 HEAD）、合并提交淡化、按摘要/作者/SHA 搜索过滤
- 提交记录页简化：导航列去掉泳道与冗余标签，完整拓扑一键进入图谱页查看，两页无损往返
- 文件历史（按路径过滤）与文件追溯（blame）独立成页，支持工作区未提交行标注与编码切换

工作流 Runbook Studio

- 工作流模板编辑器重构为分步向导：基本信息 → 操作步骤 → 变量设置 → 完成四步引导；步骤卡片化编辑、新增步骤选择器（搜索 + 常用/高级分组）、变量卡片管理与 ${变量} 引用实时分析
- 工作流模板支持 AI 生成/编辑：用中文描述需求，AI 参考使用文档直接生成或修改模板并回填表单

AI 辅助

- 分支对比 AI Code Review：Diff-first Agentic 评审覆盖全部差异文件，模型按需调用 read_lines / read_diff / get_file_tree / get_file_history / get_blame / search_code 内置工具深入仓库代码，工具调用轨迹时间线展示，评审记录本地保存可查历史
- 统一 AI 生成交互：commit message 生成、冲突合并建议、工作流模板生成共用「AI 思考弹窗」，思维链流式实时展示，支持后台运行
- AI 可靠性：流式输出被网关截断时如实报错不再当作成功（冲突合并建议不再出现半截草稿）；默认输出上限提升，推理模型不再频繁触发截断；三处 AI 生成互斥防并发串扰
- Office 文档文本化差异：docx / xlsx / pptx 提取正文文本做行级差异对比，工作区、历史、贮藏、分支比较四个差异视图共用

凭据与网络

- OAuth 快速登录：GitHub 设备流 + Gitee 授权码流，浏览器授权后自动保存
- Gitee OAuth 令牌自动续期：access_token 过期前经 broker 自动刷新，无需反复重新登录
- 凭据测试：测试前弹窗确认测试地址（预填记录地址、可改），HTTPS 凭据校验同站点防止令牌误用
- 系统代理 / 自定义代理（HTTP、HTTPS、SOCKS5）全局生效

差异查看

- 全文/紧凑切换、语法高亮（深浅主题自适应）、编码识别与手动切换（UTF-8 / GB18030 / Big5）
- Tab 缩进的文件（Java、Python 等老项目）在差异/追溯/浏览视图中按 4 列展开正常显示缩进
- 子目录中的未跟踪文件正确展示完整内容

更新与安装

- 更新系统：新版本弹窗与更新设置页展示版本说明；「关于」页展示当前版本与渠道；用户级安装无需管理员权限
- 测试版（Beta）更新渠道：在更新设置中按需开启，未开启的用户仅收到正式版

其他

- 数十项稳定性修复：凭据并发安全、大文件内存防护、列表虚拟化性能、弹窗交互细节等
