Khaslana 3.0.0-beta.1

3.0 的第一个测试版：主要界面迁移至 GPUI Kit 组件体系，代码索引内置 MCP 服务器开放给 AI 工具，新增 Ctrl+P 全局符号搜索，更新检查转为后台周期静默执行。

MCP 代码索引服务

- 新增 MCP 查询服务器：Claude Code、Cursor、ZCode 等 AI 工具可通过 MCP 直接查询本机仓库的代码索引；khaslana mcp 为多仓库模式（工具按 repo 参数选择目标仓库），khaslana mcp <仓库路径> 固定单仓库
- MCP 会话中首次使用某仓库时自动建立或增量刷新索引，无需预先手动索引，MCP 配置不与仓库路径耦合
- 设置中心「代码索引」页重构：新增「MCP 服务器」分组展示接入说明、启动命令与一键复制 MCP 配置；仓库列表支持按仓库开关索引、显示状态徽标与重建/删除入口
- 修复 Rust 导入解析失效、增量索引残留孤儿模块、索引 panic 后状态未复位等问题，导入清洗去除噪声

符号搜索

- 新增 Ctrl+P 全局符号搜索面板：输入即查代码索引，双栏展示结果列表与选中符号的调用关系详情，Enter 打开内置追溯视图
- ↑↓ 切换结果、Esc 关闭，查询与详情异步加载且乱序结果自动丢弃

界面重构

- 主要界面迁移至 GPUI Kit 组件体系：按钮、开关、输入框、菜单、对话框与列表统一为 Kit 组件和规范交互
- 设置中心重构：设置项统一封装，AI 设置从独立弹窗迁移到设置中心
- 提交图谱页改为上下分栏布局并提取共用组件；状态栏布局精简，AI 入口改为轻量样式

窗口外观

- 圆角无边框窗口改用系统样式位保留方案：拖拽缩放、Aero Snap 贴靠、双击顶边最大化等系统行为恢复正常，窗口边缘缩放与原生一致

自动更新

- 新增 12 小时周期静默更新检查：发现新版本弹出可点击气泡；手动检查弹气泡反馈，启动检查仅在状态栏提示；自动检查设置实时生效
